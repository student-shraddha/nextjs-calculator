version: 0.0
os: linux
files:
  - source: /
    destination:/opt/codedeploy-agent/deployment-root/95d408f8-8fe9-4c82-ba0f-4abe4e50897c  # Adjust this path to where you want to deploy

hooks:
  BeforeInstall:
    - location: scripts/before_install.sh
      timeout: 300
      runas: ec2-user
  AfterInstall:
    - location: scripts/after_install.sh
      timeout: 300
      runas: ec2-user
